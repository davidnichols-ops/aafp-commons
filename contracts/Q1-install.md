# Q1 Install Contract

Document installation from the private GitHub repository using both SSH and
HTTPS `uv tool install` commands. Include the module MCP invocation and an
isolated `COMMONS_HOME=/tmp/commons-pass4-*` check. Keep long instructions in
`docs/INSTALL.md` and leave README at or below 80 lines.
